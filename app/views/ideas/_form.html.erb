<div class="row">
  <%= form_with(model: idea, local: true) do |form| %>
    <% if idea.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(idea.errors.count, "error") %> prohibited this idea from being saved:</h2>

        <ul>
        <% idea.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  <div class="col s9">
    <div class="field">
      <%= form.label :content %>
      <%= form.text_field :content, id: :idea_content %>
    </div>
  </div>
</div>
  <div class="row">
  <div class="col s4">
     <%= form.collection_select :category_id, Category.all, :id, :name, { prompt: "Choose a category" }, class: "browser-default" %>

  </div class="col s2">
    <div class="btn waves-effect waves-light">
  <i class="material-icons right"><%= form.submit %></i>
    </div>

</button>
  </div>
</div>
  <% end %>
</div>
